export interface NavbarStyles {
  isActive: boolean;
}

export type routeNames = "/posts" | "/login" | "/register";

export interface routeTypes {
  HOME: "/posts";
  LOGIN: "/login";
  REGISTER: "/register";
}
