export interface NavbarStyles {
    isActive: boolean
}

export type routeNames = '/' | '/login' | '/register'

export interface routeTypes  {
    HOME: '/';
    LOGIN: '/login';
    REGISTER: '/register';
}