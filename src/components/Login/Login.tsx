import * as React from 'react'
import * as Styled from './LoginStyles'

const Login = () => {
    return (
        <Styled.Login>
            Login
        </Styled.Login>
    )
}

export default Login